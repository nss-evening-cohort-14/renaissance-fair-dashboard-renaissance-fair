// USE WITH FIREBASE AUTH
import checkLoginStatus from './helpers/auth';
import '../styles/main.scss';
import 'bootstrap';

const init = () => {
  // USE WITH FIREBASE AUTH
  checkLoginStatus();
};

init();
